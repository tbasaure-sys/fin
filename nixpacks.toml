# nixpacks.toml — Railway build configuration
# Python version is set via runtime.txt (python-3.11.8).
# No custom phases — Nixpacks handles virtualenv + pip install automatically.
# startCommand is in railway.toml.
